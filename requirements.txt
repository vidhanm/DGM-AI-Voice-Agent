# Core dependencies
python-dotenv==1.0.0
pyyaml==6.0.1
pydantic==2.5.0

# LLM providers
openai==1.6.1
anthropic==0.8.1
cerebras-cloud-sdk>=1.0.0  # Free, ultra-fast inference

# LiveKit Agents Framework for voice (v1.2+)
# Main framework with core plugins
livekit-agents==1.2.18

# LiveKit plugins for STT/TTS/VAD
livekit-plugins-deepgram==1.1.1      # Speech-to-Text (Deepgram Nova-3)
livekit-plugins-cartesia==0.2.3      # Text-to-Speech (Cartesia Sonic)
livekit-plugins-openai==0.9.5        # Alternative STT/TTS (OpenAI Whisper/TTS)
livekit-plugins-silero==0.7.2        # Voice Activity Detection (Silero VAD)
livekit-plugins-turn-detector==0.1.0 # Turn detection utilities

# LiveKit API for room management
livekit-api==0.7.0

# Database
sqlalchemy==2.0.23

# Data processing
pandas==2.1.4
numpy==1.26.2

# Logging and monitoring
structlog==23.2.0

# Testing and evaluation
pytest==7.4.3
pytest-asyncio==0.21.1

# Text processing
nltk==3.8.1
textstat==0.7.3

# Audio processing (for voice features)
pydub==0.25.1
soundfile==0.12.1

# Utilities
tqdm==4.66.1
colorama==0.4.6
rich==13.7.0

# HTTP requests
requests==2.31.0
httpx==0.25.2

# Async support
asyncio==3.4.3
aiofiles==23.2.1

# Web Framework (for web UI)
fastapi==0.104.1
uvicorn[standard]==0.24.0
python-multipart==0.0.6
websockets==12.0
